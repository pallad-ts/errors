import {Domain} from "@src/Domain";

describe('Domain', () => {

	let domain: Domain;
	beforeEach(() => {
		domain = new Domain();
	})
	describe('adding error descriptor', () => {
		it.todo('success', () => {

		});
	});
});
